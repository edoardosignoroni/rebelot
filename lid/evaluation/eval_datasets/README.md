# Evaluation datasets

This folder contains scripts to preprocess the evaluation data. To create the evaluation datasets run `make all` in this folder. This will create the evaluation datasets in the HuggingFace dataset format and store them into the folder `hf_datasets/`.
